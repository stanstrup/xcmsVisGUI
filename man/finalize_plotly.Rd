% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_reactive.R
\name{finalize_plotly}
\alias{finalize_plotly}
\title{Convert a ggplot to plotly and finalize it for an interactive plot module:
tooltip from the \code{text} aes, dynamic ticks, zoom persistence, and event
registration. \code{keep_zoom} is the function returned by \code{zoom_keeper(source)}.
Collapses the identical render tail repeated in every ggplot-based plot module.}
\usage{
finalize_plotly(gg, source, keep_zoom)
}
\description{
Convert a ggplot to plotly and finalize it for an interactive plot module:
tooltip from the \code{text} aes, dynamic ticks, zoom persistence, and event
registration. \code{keep_zoom} is the function returned by \code{zoom_keeper(source)}.
Collapses the identical render tail repeated in every ggplot-based plot module.
}

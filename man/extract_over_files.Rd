% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_extract.R
\name{extract_over_files}
\alias{extract_over_files}
\title{Run a per-file \code{extractor(path)} over the included files, attach the requested
sample metadata, optionally join scan numbers by rt, and bind into one tibble.
The extractor is wrapped with purrr::possibly so an unreadable file is SKIPPED
(its name passed to \code{on_error}) rather than aborting the whole multi-file plot.
Keeps the Shiny layer out: the caller supplies \code{on_error} for any notification.}
\usage{
extract_over_files(
  files_df,
  extractor,
  cols = "sample_id",
  scan = FALSE,
  on_error = NULL
)
}
\arguments{
\item{files_df}{included files (needs id, path, name, sample_group)}

\item{extractor}{function(path) -> tibble with an \code{rt} column}

\item{cols}{metadata to attach: any of sample_id / sample_name / sample_group}

\item{scan}{join acquisition numbers by rt (MS map)}

\item{on_error}{function(failed_names) called once if any file failed}
}
\description{
Run a per-file \code{extractor(path)} over the included files, attach the requested
sample metadata, optionally join scan numbers by rt, and bind into one tibble.
The extractor is wrapped with purrr::possibly so an unreadable file is SKIPPED
(its name passed to \code{on_error}) rather than aborting the whole multi-file plot.
Keeps the Shiny layer out: the caller supplies \code{on_error} for any notification.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_extract.R
\name{.spectra_cache}
\alias{.spectra_cache}
\title{Cached per-path base Spectra object (MsBackendMzR, lazy). Single-file views
(Spectrum, MS map, Precursors) slice/filter from this instead of re-running
\code{Spectra(path, MsBackendMzR())} — a backend header read — on every rt/scan
tweak. Filtering returns new subset objects, so the cached object is never
mutated. Mirrors \code{.scan_cache}.}
\usage{
.spectra_cache
}
\description{
Cached per-path base Spectra object (MsBackendMzR, lazy). Single-file views
(Spectrum, MS map, Precursors) slice/filter from this instead of re-running
\code{Spectra(path, MsBackendMzR())} — a backend header read — on every rt/scan
tweak. Filtering returns new subset objects, so the cached object is never
mutated. Mirrors \code{.scan_cache}.
}

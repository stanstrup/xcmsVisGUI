% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_settings_store.R
\name{save_settings}
\alias{save_settings}
\title{Write the persisted subset of \code{settings} (a plain list, e.g. from
reactiveValuesToList(rv$settings)) to disk, creating the config dir. Failures
warn rather than error — saving settings must never crash the app.}
\usage{
save_settings(settings)
}
\description{
Write the persisted subset of \code{settings} (a plain list, e.g. from
reactiveValuesToList(rv$settings)) to disk, creating the config dir. Failures
warn rather than error — saving settings must never crash the app.
}

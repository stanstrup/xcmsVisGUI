% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_filters.R
\name{make_filter}
\alias{make_filter}
\title{Build a filter list from the mod_filter UI inputs. rt inputs are in \code{unit}
(display) and stored in seconds; blank/non-finite numerics become NA (no
constraint); ms_level "all"/blank -> NA. Keeps the input->filter coercion in
one place so adding a field touches the schema and the appliers only.}
\usage{
make_filter(inputs, unit)
}
\description{
Build a filter list from the mod_filter UI inputs. rt inputs are in \code{unit}
(display) and stored in seconds; blank/non-finite numerics become NA (no
constraint); ms_level "all"/blank -> NA. Keeps the input->filter coercion in
one place so adding a field touches the schema and the appliers only.
}

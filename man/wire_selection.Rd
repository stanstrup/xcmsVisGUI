% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_reactive.R
\name{wire_selection}
\alias{wire_selection}
\title{Wire a plotly click on \code{source} to \code{rv$selection} (drives the linked Spectrum
view). \code{file_id} comes from the click \code{key} aesthetic; \code{mz_from(ev)} yields the
m/z (default NA). Call ONCE inside a moduleServer. \code{suppressWarnings} hides the
benign "source not registered" notice emitted before the first render.}
\usage{
wire_selection(source, plot, rv, mz_from = function(ev) NA_real_)
}
\description{
Wire a plotly click on \code{source} to \code{rv$selection} (drives the linked Spectrum
view). \code{file_id} comes from the click \code{key} aesthetic; \code{mz_from(ev)} yields the
m/z (default NA). Call ONCE inside a moduleServer. \code{suppressWarnings} hides the
benign "source not registered" notice emitted before the first render.
}
